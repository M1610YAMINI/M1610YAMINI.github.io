git init
git add  .

git status
git config --global user.name "M1610YAMINI"
git config --global user.email "YNI789281@GMAIL.COM"
git commit -m "Initial Commit"
   	(message)
git remote add origin https://github.com/M1610YAMINI/M1610YAMINI.github.io.git
		(variable..origin)
git push -u origin master